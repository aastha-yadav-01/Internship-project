import { SettingsPage } from './pages/SettingsPage/SettingsPage';

/**
 * Root application component.
 * Renders the Settings page as the primary view.
 */
export function App() {
  return <SettingsPage />;
}
